{
  "name": "users-list-tutorial",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",g
  "scripts": {
    "start": "json-server ./db.json --port 3333 --routes routes.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.12.0"
  }
}
